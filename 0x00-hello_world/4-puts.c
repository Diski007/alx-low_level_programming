#include <stdio.h>
 /**
  * main - Using the puts funtion rather than using printf
  * Return: 0 (Success)
  */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
